def longestcommon(str):

    prefix=str(0)
    for s in str[1:]:
        while not s.startswitch(prefix):
            prefix=prefix[:-1] 

            if prefix=="":
                return""
    return prefix