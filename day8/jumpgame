def canjump(nums):
    maxreach=0
    for i in range(len(nums)):
        if i>maxreach:
            return False
        maxreach=max(maxreach,i+nums[i])
        if maxreach>=len(nuns)-1:
            return true
        
#main
nums=[2,3,1,1,0,0,1]

print(canjump(nums))